# دليل اختبار رفع العروض الفنية والمالية

## 4 سيناريوهات الاختبار

---

## ✅ السيناريو 1: تحميل العروض الفنية والمالية

### المتطلبات:

- متصفح الويب
- حساب متعهد/مزود
- مناقصة منشورة ومفتوحة

### الخطوات:

1. **تسجيل الدخول كمزود**

   ```
   الرابط: https://app.com/login
   البريد: supplier@example.com
   ```

2. **البحث عن المناقصة**

   ```
   انقر على: "المناقصات المتاحة"
   اختر: مناقصة منشورة
   ```

3. **إرسال عرض**

   ```
   انقر على: "إرسال عرض"
   ```

4. **ملء بيانات العرض**

   ```
   ✓ المبلغ الإجمالي: 50,000 TND
   ✓ مدة التسليم: 30 يوم
   ✓ شروط الدفع: 50% + 50%
   ```

5. **تحميل الملفات**

   ```
   العرض الفني:
   - اضغط "اختر ملف"
   - حمل ملف PDF بصيغة: "عرض_فني_الشركة.pdf"
   - تحقق من الرفع (✓ تم الرفع)

   العرض المالي:
   - اضغط "اختر ملف"
   - حمل ملف Excel: "عرض_مالي.xlsx"
   - تحقق من الرفع (✓ تم الرفع)
   ```

6. **التشفير (إن أمكن)**

   ```
   ☑️ هل تريد تشفير العروض؟
   ☐ نعم (اختياري - للعروض السرية جداً)
   ✓ لا (الخيار الافتراضي)
   ```

7. **المراجعة والإرسال**
   ```
   ✓ تحقق من جميع البيانات
   ✓ اقرأ الشروط والأحكام
   ✓ اضغط "إرسال العرض بشكل نهائي"
   ```

### النتائج المتوقعة:

```
✅ رسالة نجاح: "تم استقبال عرضك بنجاح"
✅ رقم العرض: يُعطى (مثل: OFFER-2025-00123)
✅ ملفات تم رفعها بنجاح
✅ حالة العرض: مستقبل/pending
✅ تاريخ الاستقبال: يُسجل تلقائياً
```

### التحقق من قاعدة البيانات:

```sql
SELECT
  offer_number,
  total_amount,
  technical_proposal,
  financial_proposal,
  status,
  submitted_at
FROM offers
WHERE supplier_id = [supplier_id]
ORDER BY submitted_at DESC LIMIT 1;
```

**النتائج يجب أن تظهر:**

- ✓ `technical_proposal` مملوء
- ✓ `financial_proposal` مملوء
- ✓ `status = 'submitted'`
- ✓ `submitted_at` قريب من الآن

---

## ✅ السيناريو 2: منع التعديل بعد التحميل

### المتطلبات:

- عرض تم إرساله في السيناريو 1
- نفس حساب المزود

### الخطوات:

1. **عرض العرض المُرسل**

   ```
   انقر على: "عروضي"
   اختر: العرض الذي أرسلته
   ```

2. **محاولة التعديل**

   ```
   حاول الضغط على:
   - تعديل المبلغ ❌ (معطّل)
   - حذف الملفات ❌ (معطّل)
   - تعديل شروط الدفع ❌ (معطّل)
   ```

3. **التحقق من الحظر**

   ```
   ✓ جميع الحقول معطّلة (disabled)
   ✓ الأزرار غير قابلة للضغط
   ✓ رسالة: "لا يمكن تعديل العرض بعد الإرسال النهائي"
   ```

4. **محاولة الحذف**

   ```
   حاول الضغط على: "حذف العرض"

   النتيجة:
   ❌ العرض لا يُحذف
   ✓ رسالة خطأ: "لا يمكن حذف عرض مُرسل"
   ```

### النتائج المتوقعة:

```
✅ جميع حقول التعديل معطّلة
✅ رسالة واضحة للمستخدم
✅ العرض محفوظ بدون تغيير
✅ حالة العرض: submitted (لا تتغير)
```

### التحقق من قاعدة البيانات:

```sql
SELECT
  status,
  updated_at,
  is_locked
FROM offers
WHERE id = [offer_id];
```

**النتائج:**

- ✓ `status = 'submitted'` (ثابت)
- ✓ `is_locked = true` (مقفول)
- ✓ `updated_at` لم يتغير منذ الإرسال

---

## ✅ السيناريو 3: الإغلاق التلقائي الصارم

### المتطلبات:

- مناقصة بموعد إغلاق معروف
- توقيت دقيق للاختبار

### الخطوات:

1. **اختيار مناقصة قريبة من الإغلاق**

   ```
   ابحث عن مناقصة:
   - موعد الإغلاق: اليوم 10:00:00
   - الوقت الحالي: 09:59:50 (قبل الإغلاق ب 10 ثواني)
   ```

2. **محاولة إرسال عرض قبل الإغلاق**

   ```
   اضغط: "إرسال عرض جديد"
   ملء البيانات بسرعة
   اضغط: "إرسال"

   النتيجة: ✅ العرض يُقبل
   ```

3. **محاولة إرسال عرض بعد الإغلاق**

   ```
   انتظر حتى: 10:00:05 (بعد الإغلاق ب 5 ثواني)

   حاول إرسال عرض جديد
   اضغط: "إرسال"

   النتيجة: ❌ رسالة خطأ قاسية
   ```

4. **الخطأ المتوقع:**
   ```
   ❌ "مأسف! موعد الإغلاق لهذه المناقصة قد انتهى"
   ❌ "لا يمكن قبول عروض بعد 10:00:00"
   ❌ "الوقت الحالي: 10:00:05"
   ```

### النتائج المتوقعة:

```
✅ العروض قبل الموعد: تُقبل ✓
✅ العروض بعد الموعد: تُرفض ✗
✅ النظام صارم جداً (لا توسع للموعد)
✅ الرسالة واضحة للمستخدم
```

### التحقق من قاعدة البيانات:

```sql
SELECT
  offer_number,
  submitted_at,
  tender_deadline,
  CASE
    WHEN submitted_at > tender_deadline THEN 'AFTER'
    WHEN submitted_at <= tender_deadline THEN 'BEFORE'
  END as deadline_status
FROM offers
WHERE tender_id = [tender_id]
ORDER BY submitted_at DESC
LIMIT 10;
```

**النتائج:**

- ✓ العروض بعد الموعد: `deadline_status = 'AFTER'` → لا توجد (مرفوضة)
- ✓ العروض قبل الموعد: `deadline_status = 'BEFORE'` → موجودة (مقبولة)

---

## ✅ السيناريو 4: شهادة الإيداع (Certificat d'Dépôt)

### المتطلبات:

- عرض تم إرساله بنجاح
- نفس حساب المزود

### الخطوات:

1. **فتح صفحة العرض المُرسل**

   ```
   انقر على: "عروضي"
   اختر: آخر عرض أرسلته
   ```

2. **البحث عن الشهادة**

   ```
   ✓ رقم الشهادة: يظهر بوضوح
   ✓ تاريخ الإيداع: يظهر بوضوح
   ✓ الساعة الدقيقة: تظهر
   ✓ رقم المناقصة: تظهر
   ✓ المبلغ الإجمالي: يظهر
   ```

3. **مثال على الشهادة:**

   ```
   ╔══════════════════════════════════════╗
   ║   شهادة استقبال العرض - محفوظة رقمياً  ║
   ║   Certificat d'Dépôt de l'Offre       ║
   ╚══════════════════════════════════════╝

   رقم الشهادة: REC-2025-001-A7K9
   تاريخ الاستقبال: 24/11/2025
   الساعة: 14:35:42
   المنطقة الزمنية: UTC+1

   بيانات العرض:
   - المناقصة: CONS-2025-001
   - رقم العرض: OFFER-2025-123
   - المزود: شركة النجاح للمقاولات
   - المبلغ: 50,000 TND
   - مدة التسليم: 30 يوم

   الملفات المستقبلة:
   ✓ عرض_فني.pdf (2.5 MB)
   ✓ عرض_مالي.xlsx (1.2 MB)

   التوقيع الرقمي:
   SHA-256: a7b8c9d0e1f2g3h4i5j6k7l8m9n0o1p2

   هذه الشهادة تثبت استقبال عرضك بنجاح
   ```

4. **تحميل الشهادة**

   ```
   اضغط على: "تحميل الشهادة" أو "Download Receipt"

   النتيجة:
   ✓ ملف PDF يُحمّل تلقائياً
   ✓ اسم الملف: REC-2025-001-A7K9.pdf
   ```

5. **الإشعار بالبريد**

   ```
   يجب أن تستقبل بريد إلكتروني:

   الموضوع: "تم استقبال عرضك بنجاح"
   المحتوى:
   - رقم الشهادة
   - تاريخ الاستقبال
   - رابط تحميل الشهادة
   - بيانات المناقصة
   ```

### النتائج المتوقعة:

```
✅ شهادة إيداع تُظهر على الفور
✅ الشهادة تحتوي على جميع البيانات الصحيحة
✅ الشهادة قابلة للتحميل (PDF)
✅ إشعار بريد إلكتروني يصل
✅ الشهادة موقّعة رقمياً
✅ تاريخ وساعة دقيقة مسجلة
```

### التحقق من قاعدة البيانات:

```sql
SELECT
  offer_number,
  receipt_number,
  receipt_issued_at,
  receipt_digital_signature,
  receipt_file_url
FROM offers
WHERE id = [offer_id];
```

**النتائج:**

- ✓ `receipt_number` = REC-YYYY-XXX-XXXX
- ✓ `receipt_issued_at` قريب من submitted_at
- ✓ `receipt_digital_signature` موجود (hash)
- ✓ `receipt_file_url` يشير إلى ملف PDF

---

## 🎯 ملخص المتطلبات

| الميزة             | المتطلب                | الحالة |
| ------------------ | ---------------------- | ------ |
| تحميل العرض الفني  | ملف PDF أو Word        | ✅     |
| تحميل العرض المالي | ملف Excel أو PDF       | ✅     |
| التشفير الاختياري  | خيار checkbox          | ✅     |
| حظر التعديل        | بعد الإرسال النهائي    | ✅     |
| حظر الحذف          | بعد الإرسال النهائي    | ✅     |
| إغلاق صارم         | في موعد الإغلاق تماماً | ✅     |
| شهادة إيداع        | رقمية مع توقيع         | ✅     |
| إشعار بريد         | عند الاستقبال          | ✅     |

---

## ⚠️ حالات الأخطاء الشائعة

| المشكلة                | السبب            | الحل                      |
| ---------------------- | ---------------- | ------------------------- |
| لا يمكن رفع الملفات    | صيغة غير مدعومة  | استخدم PDF/Word/Excel فقط |
| العرض يُرفض قبل الموعد | خطأ في التوقيت   | تحقق من المنطقة الزمنية   |
| الشهادة لا تظهر        | JavaScript معطّل | فعّل JavaScript           |
| الملفات لا تُحمّل      | مشكلة تشفير      | استخدم تشفير مدعوم        |

---

## ✅ قائمة التحقق النهائية

- [ ] تحميل ملفات فنية ومالية بنجاح
- [ ] الملفات محفوظة بشكل صحيح
- [ ] لا يمكن تعديل العرض بعد الإرسال
- [ ] لا يمكن حذف العرض بعد الإرسال
- [ ] العروض المتأخرة تُرفض تلقائياً
- [ ] شهادة إيداع رقمية تُصدر فوراً
- [ ] الشهادة تحتوي على توقيع رقمي
- [ ] بريد إلكتروني يصل بالشهادة
- [ ] الشهادة قابلة للتحميل (PDF)
