# ✅ قائمة التحقق الشاملة - نظام الاستفسارات والإلحاقات

## 🎯 اختبار جميع الميزات الأربع

---

## ✅ الميزة 1: إرسال الاستفسار (المتعهد)

### المتطلبات الأساسية:

- [ ] حساب متعهد نشط
- [ ] مناقصة منشورة على النظام
- [ ] اتصال إنترنت مستقر

### اختبارات الإرسال:

- [ ] إرسال استفسار بموضوع فقط (بدون نص) → رسالة خطأ
- [ ] إرسال استفسار بنص فقط (بدون موضوع) → رسالة خطأ
- [ ] إرسال استفسار كامل (موضوع + نص) → نجاح
- [ ] الاستفسار يظهر في القائمة بحالة "قيد الانتظار"
- [ ] معرّف فريد يُعطى للاستفسار
- [ ] تاريخ الإرسال يُسجل بشكل صحيح
- [ ] يمكن إرسال استفسارات متعددة
- [ ] الاستفسارات تُرتب بالأحدث أولاً

### اختبارات قاعدة البيانات:

- [ ] تحقق من جدول `tender_inquiries`
  ```sql
  SELECT * FROM tender_inquiries ORDER BY submitted_at DESC LIMIT 1;
  ```

  - [ ] `id` موجود وفريد
  - [ ] `tender_id` صحيح
  - [ ] `supplier_id` يطابق المتعهد الحالي
  - [ ] `status` = `pending`
  - [ ] `submitted_at` قريب من الوقت الحالي

### اختبارات الأمان:

- [ ] لا يمكن إرسال استفسار بدون تسجيل دخول
- [ ] لا يمكن رؤية استفسارات مناقصة أخرى
- [ ] لا يمكن تعديل استفسار بعد الإرسال

---

## ✅ الميزة 2: معالجة الردود (صاحب المناقصة)

### المتطلبات الأساسية:

- [ ] حساب صاحب المناقصة (Buyer) نشط
- [ ] استفسار واحد على الأقل بحالة `pending`
- [ ] صلاحيات إدارة المناقصة

### اختبارات الرد:

- [ ] رؤية جميع الاستفسارات على المناقصة
- [ ] الاستفسارات مُرتبة بشكل واضح
- [ ] حالة كل استفسار مرئية (pending/answered)
- [ ] الرد على استفسار بدون نص → رسالة خطأ
- [ ] الرد على استفسار بنص مكتمل → نجاح
- [ ] الرد يظهر تحت الاستفسار فوراً
- [ ] اسم صاحب الرد مسجل بشكل صحيح
- [ ] تاريخ الرد يُسجل بشكل صحيح
- [ ] حالة الاستفسار تتغير من "pending" إلى "answered"
- [ ] يمكن إضافة ردود متعددة على نفس الاستفسار

### اختبارات قاعدة البيانات:

- [ ] تحقق من جدول `inquiry_responses`

  ```sql
  SELECT * FROM inquiry_responses ORDER BY answered_at DESC LIMIT 1;
  ```

  - [ ] `id` موجود وفريد
  - [ ] `inquiry_id` يطابق الاستفسار
  - [ ] `response_text` يحتوي على الرد
  - [ ] `answered_by` = buyer_id
  - [ ] `is_public` = true

- [ ] تحقق من تحديث الاستفسار:
  ```sql
  SELECT status FROM tender_inquiries WHERE id = [inquiry_id];
  ```

  - [ ] `status` = `answered`

### اختبارات الأمان:

- [ ] لا يمكن الرد بدون صلاحيات إدارة
- [ ] لا يمكن رؤية ردود استفسارات مناقصة أخرى
- [ ] الردود الخاصة مخفية إذا كانت `is_public = false`

---

## ✅ الميزة 3: نشر الملحق (Addendum)

### المتطلبات الأساسية:

- [ ] استفسار واحد على الأقل مع رد
- [ ] صاحب المناقصة مسجل دخول
- [ ] محتوى توضيحي جاهز

### اختبارات النشر:

- [ ] نشر ملحق بدون عنوان → رسالة خطأ
- [ ] نشر ملحق بدون محتوى → رسالة خطأ
- [ ] نشر ملحق كامل (عنوان + محتوى) → نجاح
- [ ] الملحق يظهر في قائمة الملاحق فوراً
- [ ] رقم فريد يُعطى للملحق (ADD-YYYY-XXXXX)
- [ ] النسخة = 1 للملحق الأول
- [ ] تاريخ النشر يُسجل بشكل صحيح
- [ ] اسم ناشر الملحق مسجل
- [ ] نشر ملحق ثاني يحصل على نسخة = 2
- [ ] الملحق قابل للتحميل (بصيغة نصية)
- [ ] محتوى الملحق المُحمّل يطابق المحفوظ

### اختبارات قاعدة البيانات:

- [ ] تحقق من جدول `addenda`
  ```sql
  SELECT * FROM addenda WHERE tender_id = [tender_id] ORDER BY published_at DESC;
  ```

  - [ ] `id` موجود وفريد
  - [ ] `addendum_number` بصيغة `ADD-YYYY-XXXXX`
  - [ ] `addendum_number` فريد (UNIQUE)
  - [ ] `version` = 1 للأول، 2 للثاني، إلخ
  - [ ] `published_by` = buyer_id
  - [ ] `published_at` قريب من الوقت الحالي

### اختبارات النسخ المتعددة:

- [ ] نشر ملحق أول

  ```sql
  SELECT version FROM addenda WHERE tender_id = [id] ORDER BY version DESC LIMIT 1;
  ```

  - [ ] `version` = 1

- [ ] نشر ملحق ثاني
  - [ ] `version` = 2
  - [ ] `addendum_number` مختلف عن الأول

### اختبارات الأمان:

- [ ] لا يمكن نشر ملحق بدون صلاحيات
- [ ] لا يمكن نشر ملحق على مناقصة ليست ملكك

---

## ✅ الميزة 4: الإشعارات التلقائية

### المتطلبات الأساسية:

- [ ] ملحق تم نشره للتو
- [ ] متعهد قدم استفسار على المناقصة
- [ ] قائمة بريد صحيحة

### اختبارات الإرسال الفوري:

- [ ] عند نشر ملحق، يُرسل إشعار فوراً
- [ ] الانتظار لمدة 5 ثواني
- [ ] فتح مركز الإشعارات
  - [ ] ظهور إشعار جديد بعنوان واضح
  - [ ] الإشعار يحتوي على اسم المناقصة
  - [ ] الإشعار يحتوي على رقم الملحق
  - [ ] تاريخ النشر مرئي

### اختبارات المحتوى:

- [ ] الإشعار يحتوي على:
  - [ ] اسم المناقصة (tender_title)
  - [ ] رقم المناقصة (tender_number)
  - [ ] رقم الملحق (addendum_number)
  - [ ] عنوان الملحق
  - [ ] تاريخ النشر
  - [ ] رسالة واضحة بأن ملحق جديد متاح

### اختبارات الفتح والتحميل:

- [ ] انقر على الإشعار
  - [ ] فتح صفحة الملحق
  - [ ] عرض محتوى الملحق الكامل
- [ ] انقر على "تحميل"
  - [ ] تحميل ملف بصيغة نصية
  - [ ] اسم الملف يحتوي على رقم الملحق

### اختبارات تحديد الإشعار كمقروء:

- [ ] الإشعار يظهر بـ "جديد" (غير مقروء)
- [ ] انقر على "تحديد كمقروء"
  - [ ] تحديث الإشعار
  - [ ] اختفاء علامة "جديد"
- [ ] تحديث الصفحة
  - [ ] الإشعار يبقى مقروءاً

### اختبارات قاعدة البيانات:

- [ ] تحقق من جدول `addendum_notifications`

  ```sql
  SELECT * FROM addendum_notifications
  WHERE addendum_id = [addendum_id]
  ORDER BY sent_at DESC;
  ```

  - [ ] `id` موجود وفريد
  - [ ] `addendum_id` يطابق الملحق
  - [ ] `user_id` يطابق المتعهد
  - [ ] `sent_at` قريب من وقت نشر الملحق
  - [ ] `read_at` = null إذا لم يُقرأ
  - [ ] `notification_method` = email أو platform

- [ ] بعد تحديد الإشعار كمقروء:
  ```sql
  SELECT read_at FROM addendum_notifications WHERE id = [notif_id];
  ```

  - [ ] `read_at` ≠ null
  - [ ] `read_at` قريب من الوقت الحالي

### اختبارات متعددة المتعهدين:

- [ ] نشر ملحق
- [ ] تسجيل دخول 2-3 متعهدين مختلفين
- [ ] التحقق من وصول الإشعار لكل واحد منهم
- [ ] كل متعهد له إشعار منفصل

---

## 🔄 اختبار الدورة الكاملة (End-to-End)

### السيناريو الكامل:

```
1. ✅ متعهد 1: إرسال استفسار
   └─ التحقق: استفسار محفوظ بـ ID = 5

2. ✅ متعهد 2: إرسال استفسار آخر
   └─ التحقق: استفسار محفوظ بـ ID = 6

3. ✅ صاحب المناقصة: الرد على الاستفسار 5
   └─ التحقق: استفسار 5 حالته = answered

4. ✅ صاحب المناقصة: الرد على الاستفسار 6
   └─ التحقق: استفسار 6 حالته = answered

5. ✅ صاحب المناقصة: نشر ملحق
   └─ التحقق: ملحق بـ ID = 1، رقم ADD-2025-XXXXX

6. ✅ متعهد 1: استقبال إشعار
   └─ التحقق: إشعار جديد في المركز

7. ✅ متعهد 1: فتح الملحق
   └─ التحقق: محتوى الملحق مرئي

8. ✅ متعهد 1: تحميل الملحق
   └─ التحقق: ملف منزّل بنجاح

9. ✅ متعهد 1: تحديد كمقروء
   └─ التحقق: الإشعار لا يظهر كـ جديد

10. ✅ متعهد 2: استقبال إشعار
    └─ التحقق: نفس الملحق لديهم أيضاً
```

---

## 📊 ملخص القياسات

| المعيار          | المتوقع   | الفعلي | الحالة |
| ---------------- | --------- | ------ | ------ |
| وقت الاستفسار    | < 1 ثانية | -      | ⏳     |
| وقت الرد         | < 1 ثانية | -      | ⏳     |
| وقت نشر الملحق   | < 2 ثانية | -      | ⏳     |
| وقت وصول الإشعار | < 5 ثواني | -      | ⏳     |
| دقة البيانات     | 100%      | -      | ⏳     |
| عدم الأخطاء      | 0 errors  | -      | ⏳     |

---

## 🎓 ملاحظات إضافية

- تأكد من استخدام بيانات حقيقية (بريد صحيح، أرقام منطقية)
- اختبر على متصفحات مختلفة (Chrome, Firefox, Safari, Edge)
- اختبر على أجهزة مختلفة (Desktop, Tablet, Mobile)
- اختبر مع سرعات إنترنت مختلفة
- اختبر مع عدد كبير من الاستفسارات (100+)
- اختبر مع ملاحق كبيرة (10KB+)
