"تصرّف كمهندس DevOps (DevOps Engineer) مسؤول عن أتمتة بيئة التطوير المحلية (Local Development Setup)."

السياق: "الخادم الخلفي يفشل في التشغيل بسبب Port 3000 is already in use. الحل المؤقت هو استخدام lsof -ti:3000 | xargs kill -9 قبل التشغيل."

المهمة: "نفذ تعديلات في ملف package.json الخاص بالـ Backend لـ تضمين أمر تنظيف المنفذ (Port Cleanup) ضمن أمر التشغيل npm run dev، لجعله إجراءً آلياً ودائماً. تأكد من أن الأمر يعمل بصمت ولا يعرض أخطاء إذا لم يكن هناك عملية تستخدم المنفذ."

التنسيق: "قدّم مثالاً على قسم scripts في ملف package.json بعد التعديل."

JSON

{
  "name": "mynet-backend",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    // 💡 الأمر المُعدَّل لضمان إغلاق المنفذ قبل التشغيل
    "clean": "lsof -ti:3000 | xargs kill -9 2>/dev/null || echo 'Port 3000 is clean'",
    "dev": "npm run clean && node server.js" 
  },
  ...
}
التأثير: يضمن هذا أن يتم تحرير المنفذ تلقائيًا قبل كل محاولة تشغيل للخادم الخلفي، مما يحل مشكلة Port 3000 is already in use.

2. 🛡️ أمر تعزيز الواجهة الأمامية (Frontend Error Handling and Health Check)
الدور: "تصرّف كمهندس واجهة أمامية (Frontend Engineer) خبير في تجربة المستخدم (UX) ومعالجة أخطاء الشبكة."

السياق: "الواجهة الأمامية تتلقى ERR_NETWORK و [vite] server connection lost، مما يؤدي إلى واجهة مستخدم فارغة أو رسائل خطأ غير مفيدة."

المهمة: "نفذ أمرين لتحسين متانة الواجهة الأمامية:

تحسين معالجة خطأ الاتصال: قم بتطبيق منطق (Logic) في ملف Login.jsx أو axiosConfig.js للكشف عن خطأ ERR_NETWORK أو ERR_CONNECTION_REFUSED. عند اكتشافه، اعرض رسالة واضحة للمستخدم مثل: 'فشل الاتصال بالخادم. يرجى التأكد من تشغيل الخادم الخلفي (Backend) على Port 3000.'

تنفيذ فحص صحة مبدئي (Initial Health Check): قبل محاولة جلب بيانات لوحة التحكم (Dashboard)، قم بإضافة استدعاء سريع لمسار صحة الخادم (مثل GET /api/health أو GET /) لمعرفة ما إذا كان الخادم الخلفي قيد التشغيل بالفعل. إذا فشل هذا الفحص، قم بمنع عرض المحتوى المعطل حتى يتم إعادة الاتصال."

التنسيق: "قدّم كود زائف (Pseudo-code) لكتلة catch المُعدلة في دالة تسجيل الدخول."

JavaScript

// Pseudo-code في Login.jsx أو Axios Interceptor
try {
    const response = await api.post('/auth/login', credentials);
    // ... نجاح
} catch (error) {
    if (error.code === 'ERR_NETWORK' || error.message.includes('refused')) {
        // 💡 رسالة واضحة للمستخدم
        setErrorMessage("⚠️ فشل الاتصال. يرجى التحقق من تشغيل Backend على Port 3000.");
    } else {
        // ... معالجة الأخطاء الأخرى
    }
}
3. 🚦 أمر التحقق من توحيد المنافذ (Port Consistency Validation)
الدور: "تصرّف كمدقق نظام (System Auditor)."

السياق: "لاحظنا وجود استدعاءات لكل من http://localhost:3000 و http://localhost:5000 في سجلات الواجهة الأمامية."

المهمة: "تحقق من جميع ملفات الإعداد ومتغيرات البيئة في كلا المشروعين لتحديد سبب التناقض. حدد أين يتم تعريف API_BASE_URL وتأكد من أنه يشير بشكل متسق إلى المنفذ الصحيح (3000، بناءً على تفضيل الخادم الخلفي)."

المخرجات المطلوبة: "أمر موجه يجب توجيهه للمطورين لمراجعة الملفات التالية (على الأقل):

ملف الـ Backend: ملف إعداد السيرفر (مثل server.js أو .env في الـ Backend).

ملف الـ Frontend: ملف إعداد Axios أو متغيرات البيئة (مثل vite.config.js أو .env في الـ Frontend)."