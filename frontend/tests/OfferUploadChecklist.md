# ✅ قائمة التحقق الشاملة - رفع العروض الفنية والمالية

## ✅ الميزة 1: تحميل الملفات مع التشفير

### اختبارات الرفع الأساسية:
- [ ] يمكن رفع ملف PDF للعرض الفني
- [ ] يمكن رفع ملف Word للعرض الفني
- [ ] يمكن رفع ملف Excel للعرض المالي
- [ ] يمكن رفع ملف CSV للعرض المالي
- [ ] يتم عرض اسم الملف بعد الرفع
- [ ] يمكن رفع أكثر من ملف
- [ ] الملفات المرفوعة تظهر في قائمة المرفقات

### اختبارات التحقق من الحجم:
- [ ] الملفات الصغيرة (< 1MB) تُقبل
- [ ] الملفات المتوسطة (5MB) تُقبل
- [ ] الملفات الكبيرة (9.5MB) تُقبل
- [ ] الملفات التي تتجاوز 10MB تُرفض برسالة واضحة

### اختبارات التشفير:
- [ ] الخيار "تشفير العروض" موجود (checkbox)
- [ ] يمكن تفعيل التشفير
- [ ] يمكن إلغاء تفعيل التشفير
- [ ] عند تفعيل التشفير، الملفات تُشفّر
- [ ] الملفات المشفرة لا تُفتح بدون مفتاح فك التشفير

### التحقق من قاعدة البيانات:
```sql
SELECT 
  offer_number,
  technical_proposal,
  financial_proposal,
  encrypted_data,
  encryption_iv,
  decryption_key_id
FROM offers
WHERE id = [offer_id];
```
- [ ] `technical_proposal` مملوء
- [ ] `financial_proposal` مملوء
- [ ] إذا كان تشفير مفعّل: `encrypted_data` موجود
- [ ] `encryption_iv` موجود
- [ ] `decryption_key_id` موجود

---

## ✅ الميزة 2: منع التعديل بعد التحميل

### اختبارات حظر التعديل:
- [ ] قبل الإرسال النهائي: يمكن تعديل المبلغ
- [ ] قبل الإرسال النهائي: يمكن تعديل مدة التسليم
- [ ] قبل الإرسال النهائي: يمكن تعديل شروط الدفع
- [ ] قبل الإرسال النهائي: يمكن استبدال الملفات

### اختبارات بعد الإرسال النهائي:
- [ ] حقل المبلغ معطّل (disabled)
- [ ] حقل مدة التسليم معطّل
- [ ] حقل شروط الدفع معطّل
- [ ] زر "استبدال الملفات" معطّل
- [ ] لا يمكن حذف أي ملف

### اختبارات الرسائل:
- [ ] رسالة واضحة: "لا يمكن تعديل العرض بعد الإرسال النهائي"
- [ ] الرسالة باللغة العربية بالكامل
- [ ] الرسالة تظهر عند محاولة التعديل

### اختبارات محاولات الالتفاف (Bypass):
- [ ] محاولة تعديل عبر F12/DevTools → محظور
- [ ] محاولة إرسال بيانات مباشرة للـ API → محظور
- [ ] محاولة حذف العرض → محظور
- [ ] محاولة تعديل عبر متصفح آخر → محظور

### التحقق من قاعدة البيانات:
```sql
SELECT 
  status,
  is_locked,
  updated_at
FROM offers
WHERE id = [offer_id];
```
- [ ] `status = 'submitted'` (لا يتغير)
- [ ] `is_locked = true`
- [ ] `updated_at` ثابت (لا يتغير عند محاولة التعديل)

---

## ✅ الميزة 3: الإغلاق التلقائي الصارم

### اختبارات الموعد الطبيعي:
- [ ] عرض مُرسل قبل الموعد يُقبل
- [ ] عرض مُرسل في الموعد تماماً يُقبل
- [ ] عرض مُرسل بعد الموعد ب 1 ثانية يُرفض

### اختبارات الرفض:
- [ ] رسالة خطأ واضحة عند الرفض
- [ ] الرسالة تتضمن: "موعد الإغلاق انتهى"
- [ ] الرسالة تتضمن: الوقت الحالي
- [ ] الرسالة تتضمن: موعد الإغلاق الصحيح

### اختبارات المنطقة الزمنية:
- [ ] النظام يستخدم المنطقة الزمنية الصحيحة
- [ ] الوقت المعروض يطابق التوقيت المحلي

### اختبارات الحدود:
- [ ] عرض مُرسل قبل الموعد ب 1 ثانية → يُقبل
- [ ] عرض مُرسل في الموعد تماماً → يُقبل
- [ ] عرض مُرسل بعد الموعد ب 1 ثانية → يُرفض ✓ (صارم!)

### التحقق من قاعدة البيانات:
```sql
SELECT 
  offer_number,
  submitted_at,
  tender_deadline,
  status,
  CASE 
    WHEN submitted_at > tender_deadline THEN 'REJECTED'
    WHEN submitted_at <= tender_deadline THEN 'ACCEPTED'
  END as deadline_status
FROM offers
WHERE tender_id = [tender_id]
ORDER BY submitted_at DESC;
```
- [ ] عروض بعد الموعد: لا توجد في قاعدة البيانات (مرفوضة)
- [ ] عروض قبل الموعد: موجودة مع `status = 'submitted'`

---

## ✅ الميزة 4: شهادة الإيداع

### اختبارات ظهور الشهادة:
- [ ] الشهادة تظهر فوراً بعد إرسال العرض
- [ ] رقم الشهادة يظهر بوضوح (مثل: REC-2025-001-A7K9)
- [ ] تاريخ الاستقبال يظهر
- [ ] الساعة الدقيقة تظهر
- [ ] معلومات المناقصة تظهر
- [ ] المبلغ الإجمالي يظهر

### اختبارات محتوى الشهادة:
- [ ] الشهادة تحتوي على:
  - [ ] رقم الشهادة (فريد)
  - [ ] رقم العرض
  - [ ] رقم المناقصة
  - [ ] اسم المزود
  - [ ] المبلغ الإجمالي
  - [ ] قائمة الملفات المستقبلة
  - [ ] التوقيع الرقمي (SHA-256 hash)

### اختبارات التحميل:
- [ ] زر "تحميل الشهادة" موجود وفعّال
- [ ] الملف المُحمّل بصيغة PDF
- [ ] اسم الملف يحتوي على رقم الشهادة
- [ ] الملف المُحمّل يحتوي على كل معلومات الشهادة
- [ ] حجم الملف معقول (< 1MB)

### اختبارات البريد الإلكتروني:
- [ ] بريد إلكتروني يُرسل عند استقبال العرض
- [ ] الموضوع: "تم استقبال عرضك بنجاح"
- [ ] البريد يحتوي على:
  - [ ] رقم الشهادة
  - [ ] تاريخ الاستقبال
  - [ ] رقم المناقصة
  - [ ] المبلغ
  - [ ] رابط تحميل الشهادة
- [ ] البريد يصل خلال 5 ثواني

### اختبارات الطباعة:
- [ ] زر "طباعة" موجود
- [ ] الشهادة تُطبع بشكل صحيح
- [ ] التنسيق جميل عند الطباعة
- [ ] جميع البيانات مرئية عند الطباعة

### التحقق من قاعدة البيانات:
```sql
SELECT 
  offer_number,
  receipt_number,
  receipt_issued_at,
  receipt_digital_signature,
  receipt_file_url,
  EXTRACT(EPOCH FROM (receipt_issued_at - submitted_at)) as seconds_to_receipt
FROM offers
WHERE id = [offer_id];
```
- [ ] `receipt_number` = REC-YYYY-XXX-XXXX (فريد)
- [ ] `receipt_issued_at` قريب جداً من `submitted_at` (< 1 ثانية)
- [ ] `receipt_digital_signature` موجود (SHA-256 hash)
- [ ] `receipt_file_url` يشير إلى ملف PDF صحيح

---

## 🔍 اختبارات الأمان

- [ ] محاولة تعديل عرض شخص آخر → محظور
- [ ] محاولة رؤية بيانات عرض مخفية → محظور
- [ ] محاولة حذف عرض → محظور
- [ ] محاولة تشفير ملف غير نظيف → معالج بأمان
- [ ] محاولة فك تشفير بدون مفتاح → فشل آمن

---

## 📊 اختبارات الأداء

- [ ] رفع ملف 10MB → < 5 ثواني
- [ ] إرسال العرض → < 2 ثانية
- [ ] ظهور الشهادة → < 1 ثانية
- [ ] تحميل الشهادة → < 3 ثواني
- [ ] إرسال البريد الإلكتروني → < 10 ثواني

---

## 🌐 اختبارات المتصفحات

- [ ] Chrome: جميع الميزات تعمل
- [ ] Firefox: جميع الميزات تعمل
- [ ] Safari: جميع الميزات تعمل
- [ ] Edge: جميع الميزات تعمل
- [ ] Mobile browsers: واجهة متجاوبة

---

## 📱 اختبارات الأجهزة

- [ ] Desktop (1920x1080): كل شيء واضح
- [ ] Tablet (768x1024): تخطيط متجاوب
- [ ] Mobile (375x812): واجهة سهلة الاستخدام

---

## ⏱️ اختبارات المتطلبات الزمنية

| المتطلب | الزمن المتوقع | الحد الأقصى |
|--------|-------------|----------|
| رفع الملف | < 5 ثواني | 10 ثواني |
| إرسال العرض | < 2 ثانية | 5 ثواني |
| ظهور الشهادة | < 1 ثانية | 3 ثواني |
| إرسال البريد | < 10 ثواني | 30 ثانية |
| تحميل الشهادة | < 3 ثواني | 10 ثواني |

---

## 🎯 ملخص الاختبار

| الميزة | المتطلبات | النسبة |
|--------|-----------|--------|
| تحميل الملفات | 10+ | 100% |
| منع التعديل | 15+ | 100% |
| الإغلاق الصارم | 8+ | 100% |
| شهادة الإيداع | 20+ | 100% |
| **الإجمالي** | **50+** | **100%** |

---

## 📝 نموذج تقرير الاختبار

```
تاريخ الاختبار: 2025-11-24
اسم المختبر: [الاسم]
الإصدار المختبر: v1.0
النظام المستخدم: [النظام]

النتائج:
✅ تحميل الملفات: [النتيجة]
✅ منع التعديل: [النتيجة]
✅ الإغلاق الصارم: [النتيجة]
✅ شهادة الإيداع: [النتيجة]

ملاحظات:
[أي ملاحظات إضافية]

التوقيع: ________________
التاريخ: ________________
```

